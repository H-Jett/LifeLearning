# CLAUDE.md — TeaLearning 开发规范

> 本文件是这个仓库的开发约定，Claude Code 会自动加载。人类或 AI 在此仓库开发时**必须遵守**。
> 目的：让这本《系统学茶：从一片叶子到一杯茶汤》保持统一、正确、可移植。
> 体例继承姊妹项目 InfraLearning / MultiModalLearning。

## 1. 这是什么

一本"边学边记"的系统性茶学入门书，深度对标**评茶员知识体系**（工艺原理 + 茶叶化学 + 感官审评），
面向零基础但要求讲透原理的读者。最终会成书，所以：

> **⚠️ 内容必须非常正确。**

姊妹书靠"真机跑数据"保证正确性；茶叶没有 GPU 可跑，**本书的正确性靠三条纪律**：

1. **数值必须有出处**：成分含量、温度、时长、限量等数字，一律标注来源
   （国标编号 / 教科书 / 明确标注为"行业经验值"），并说明波动范围。
   **禁止凭印象编造精确数字**——宁可写"典型在 x~y 区间，因品种季节差异很大"。
2. **区分「机理」与「说法」**：茶圈流传的说法很多，凡是证据不足的，写进"常见说法辨析"小节，
   明确标注**证据强度**（有共识 / 有争议 / 缺乏证据 / 明确错误），不含糊其辞。
3. **数字只当示例，讲的是规律**：读者要带走的是"为什么"，不是背表格。

**不做医疗建议**：茶与健康只陈述研究现状与证据等级，不给任何治疗、剂量、疗效承诺。

### 1.1 查证纪律（硬要求，写每一章都适用）

> **不要凭记忆写作。** 姊妹书 InfraLearning 有真机数据可跑，茶没有——
> **所以"查资料"是本书正确性的唯一来源**，不是可选项。
> 而且茶是个传说密度极高的领域，模型记忆里混进了大量茶圈流传的错误说法。

1. **写之前先查**：动笔前检索**一手资料**（国标原文、教科书、农业与食品科学论文、
   权威机构资料），而不是凭印象组织内容。**先列出本章每个数值的来源，再开始写**。
2. **一手优先，来源分级**：国标 / 学术论文 / 权威教材（如《茶叶生物化学》）>
   行业协会与科研院所资料 > 头部专业媒体 > 茶商与自媒体内容。
   **茶商文案与自媒体一律不作依据**，只能当"存在这种说法"的证据。
3. **关键结论交叉验证**：成分含量、工艺温度与时长、限量指标、品种特性，
   至少**两个独立来源**一致才写；只有一个来源时明确标注"仅见于 X"。
4. **出处要落盘**：所有引用登记进 `docs/standards.md`，正文标准只写编号不写年份。
5. **查不到就别写**：宁可写"这一点缺乏可靠数据"或改成区间描述，
   **绝不用听起来合理的措辞填补空白**。发现自己在"推测"时，停下来去查，或显式标注为推测。
6. **凡传说必辨析**：茶圈说法一律进「常见说法辨析」小节，标注证据强度
   （有共识 / 有争议 / 缺乏证据 / 明确错误），并说明它为什么流传。
7. **警惕自己的记忆**：产区划分、标准版本、工艺细节、"古法"叙事，
   **默认记忆不可靠**，一律现查。

## 2. 教学法（每章固定结构）

用**中文**写。每章遵循「**概念 → 思考题 → 实操**」：

1. 概念讲解，配机理图（Mermaid）/ 对照表，关键数值带出处；
2. 章末**思考题**（数量**按实际需求，可多可少**——简单章少几道、复杂章多几道），紧扣本章，
   指向答案册（见 §5）；
3. **本章实操**：读者目前**没有茶具茶样**，所以每个实操必须写成两档：
   - **🅐 无器具版**（纯思考 / 观察 / 查资料 / 读包装，随时能做）；
   - **🅑 有条件版**（需要盖碗、秤、温度计或茶样，标注"备齐后再做"，列清所需器具）。
   实操要给**可填写的记录表**（Markdown 表格），让读者的观察能沉淀下来。

风格：像给同事讲清楚一件事，多用"为什么"，诚实标注反直觉/局限（"诚实说明"）。

## 3. 单一真源 & 可移植 Markdown（核心原则）

- **内容只存一份**：正文 `docs/`。网页/PDF 都是它的渲染。
- **可移植 Markdown**：必须在 **GitHub 原生渲染 + 普通 Markdown 阅读器 + MkDocs** 三边都能看：
  - 用**脚注 + 引用块**做注释，**不要**用 MkDocs 专有的 `!!! note` admonition；
  - 术语表锚点、思考题跳转都用**显式 HTML 锚点** `<a id="xxx"></a>`（三边通用）；
  - 不用 `--8<--` / `{{#include}}` 这类构建期引用（纯阅读器看不到）。
- **可复用的表单**（审评记录表、冲泡记录表等）放 `docs/forms/`，各章**链接**过去，不复制粘贴。

## 4. 目录结构与命名

```
docs/                          # 正文（唯一内容真源）
  index.md                     # 封面 / 大纲 / 进度
  roadmap.md                   # 七部分学习路线图（活的大纲，改动记进"调整记录"）
  glossary.md                  # 术语表（每词条前有 <a id> 锚点，按主题分组）
  standards.md                 # 国标与文献索引（所有引用出处集中登记）
  forms/                       # 可复用的记录表（审评表、冲泡记录表…）
  chapters/NN-partname/        # 每部分一个目录
    00-intro.md                # 该部分开篇导读（每部分都要有）
    NN-name.md                 # 章节，两位数字前缀（全书连续编号）
    summary.md                 # 该部分小结（速查）
    project-PN-xxx.md          # 该部分的实战项目（见 §6）
    figures/                   # 该部分的图片
  qa/NN-name-qa.md             # 思考题答案册（网页可跳转）
scripts/check_book.py          # 校验：思考题锚点/术语锚点/绝对路径/出处标注
mkdocs.yml                     # 站点配置
.github/workflows/docs.yml     # GitHub Pages 自动发布（跑 check_book + build --strict）
```

命名对应：第 N 章 → `chapters/PART/NN-name.md`、`qa/NN-name-qa.md`。

## 5. 各类文件的具体约定

**章节 `.md`**：小节用 `## N.x 标题`。术语首次出现加脚注 `[^x]`，脚注体链接到术语表
`[术语](../../glossary.md#anchor)`。章末思考题下写跳转链接：
`> 📖 **参考答案**（想清楚再看）：[Q1](../../qa/NN-name-qa.md#q1) · [Q2](...#q2) · …`

**答案册 `docs/qa/NN-name-qa.md`**：**只记「问题 + 正确答案」**，不记读者作答。每题前加锚点
`<a id="q1"></a>`…。必须加进 `mkdocs.yml` 的「思考题参考答案」nav（否则 `--strict` 报错）。

**出处标注格式**：正文里引用标准写 `GB/T 23776`（不带年份，避免版本漂移误导），
在 `docs/standards.md` 里登记完整名称与版本，并注明"以最新发布版本为准"。
教科书引用统一简称（如《茶叶生物化学》），同样在 `standards.md` 登记。
行业经验值写明"**经验值**，不同资料略有差异"。

**术语表**：新术语加到 `glossary.md` 对应主题下，前置 `<a id="slug"></a>`。
茶叶术语要给**中文 + 国标术语原词 + 英文**（审评术语以 GB/T 14487 为准）。

## 6. 实战项目（重要：不要一章一个）

- 项目**只挂在部分末尾或关键技术节点**，同一部分**最多一个**，全书 **5 个**（P1~P5）。
- 项目文档放**所属部分目录**里（`chapters/NN-part/project-PN-xxx.md`），
  nav 排在该部分末尾，**不单独设分区**。
- 项目同样要给「无器具版 / 有条件版」两档。

## 7. 画图（Mermaid 优先）

> **随时画图**：只要图比文字更清楚，就配图——这是硬要求，别偷懒用纯文字凑。

- **工艺流程 / 分类树 / 机理链条 / 时序 → Mermaid**：```mermaid 代码块，三边都渲染、可 diff。
  茶书里最常用：六大茶类工艺流程图、成分—滋味对应图、氧化路径图、审评流程图。
- **数据图表 → matplotlib PNG**：**只在有可靠数据来源时才画**，图注必须写来源；
  没有来源就不要画成"像实测数据"的图，改用示意图并在标题里写明"示意"。
  规范：Okabe-Ito 色盲安全配色（`#0072B2 #E69F00 #D55E00 #009E73 #56B4E9 #CC79A7`）、白底、
  英文标签（避免 matplotlib 缺中文字体出方框）、细线淡网格、**不要双 Y 轴**；生成后打开图看一眼。

## 8. 新增一章的标准流程（照做）

1. 先列本章要讲的机理链条，确认每个数值有出处（查不到就改写法，别编）；
2. 写 `docs/chapters/PART/NN-name.md`（概念 + Mermaid/表 + 常见说法辨析 + 思考题 + 本章实操 🅐/🅑）；
3. 写 `docs/qa/NN-name-qa.md`（问题 + 正确答案，q1/q2/… 锚点）；
4. 新术语加进 `glossary.md`，新出处加进 `standards.md`；
5. 更新 `mkdocs.yml`（章节 nav + 答案 nav）、`docs/index.md`（进度）、`docs/roadmap.md`（状态）；
6. **校验 + 严格构建**（见 §9）全绿后再提交。

## 9. 构建与校验（提交前必做）

```bash
python scripts/check_book.py     # 思考题锚点/术语锚点/绝对路径/裸数字提醒
mkdocs build --strict            # 坏链接/警告即失败（CI 也跑这条）
```

两条全过才提交。`--strict` 失败会导致 GitHub Actions 部署失败、网页冻结——务必本地先过。

## 10. 提交与发布

- 提交信息中文、说清做了什么 + 为什么；**每完成一块可独立成立的内容就提交**，不要攒到最后。
- push 到 `main` 触发 Actions 自动构建并发布到 GitHub Pages。
- `mkdocs.yml` 配 `site_url` + `repo_url` + `repo_name`；`README.md` 顶部放**在线阅读**链接——
  网页与仓库互相可达。

## 11. 通用约束（来自用户全局规范）

- **不主动写报告文件**（`*.md` 总结、`report_*.json` 等）；结论直接说，除非明确要求导出。
- 需要时间戳用**北京时间**：`TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M'`。
- **仓库内所有文件禁止出现绝对路径**（尤其带用户名或机器目录布局的）——公开仓库会泄漏个人信息；
  只用相对路径 / 环境变量。聊天里为方便定位可给绝对路径，但**绝不提交进仓库**。
